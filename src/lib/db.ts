// Database operations are now handled via Supabase (see src/lib/supabase.ts).
// This file is kept as a no-op shim so that existing imports don't break
// until all references are cleaned up.
export {};
